# Site settings are accessed in your site via
# {{ site.title }}, {{ site.email }}, and so on.
# New ones work too, like {{ site.myvariable }}.

title: Student Database Build Metrics
email: dailey.matthew@gmail.com
description: >- # this means to ignore newlines until "baseurl:"
  This is a place for build metrics and other documentation about the
  Student Database sample application for Web Application Engineering
  at the Asian Institute of Techology in 2021.
baseurl: "/web16/web16-app"
url: "https://ait-wae-2021.gitlab.io"
github_username: mdailey
gitlab_username: dailey.matthew

# Build settings
theme: minima
plugins:
  - jekyll-feed

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
